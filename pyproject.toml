[project]
name = "ollama-chat"
version = "1.0.0"
description = "本地優先的 Ollama CLI 工具，支援多輪對話、Streaming、Fallback 模型鏈和 RAG"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
    { name = "alex", email = "alex@ai" }
]
keywords = ["ollama", "llm", "cli", "rag", "chat"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "requests>=2.28.0",
]

[project.optional-dependencies]
pdf = [
    "pypdf>=3.0.0",
]
all = [
    "pypdf>=3.0.0",
]

[project.urls]
Homepage = "https://github.com/alex/ollama-chat"
Repository = "https://github.com/alex/ollama-chat"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]

[dependency-groups]
dev = []
